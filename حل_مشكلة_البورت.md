# 🔧 حل مشكلة البورت المستخدم - Port Already in Use

## ❌ المشكلة

```
Error: listen EADDRINUSE: address already in use :::3000
```

**السبب:** البورت 3000 مستخدم بالفعل من قبل عملية أخرى.

---

## ✅ الحلول

### الحل 1: إيقاف العملية المستخدمة للبورت ⭐

```powershell
# 1. ابحث عن العملية
netstat -ano | findstr :3000

# 2. أوقف العملية (استبدل PID بالرقم الذي يظهر)
taskkill /F /PID [PID]

# مثال:
taskkill /F /PID 16380
```

**تم تنفيذه:** ✅ تم إيقاف العملية 16380

---

### الحل 2: استخدام بورت آخر

```bash
# استخدم بورت 3001 بدلاً من 3000
next start -p 3001

# أو أضف script في package.json:
npm run start:port 3001
```

---

### الحل 3: إيقاف جميع عمليات Node.js

```powershell
# إيقاف جميع عمليات node.exe
taskkill /F /IM node.exe
```

**⚠️ تحذير:** سيوقف جميع تطبيقات Node.js المفتوحة!

---

## 🚀 الآن يمكنك البدء

بعد إيقاف العملية، يمكنك الآن:

```bash
# 1. تأكد من أنك بنيت المشروع
npm run build

# 2. ابدأ Production Server
npm start

# 3. افتح المتصفح
# http://localhost:3000
```

---

## 📋 نصائح

### 1. تحقق من البورت قبل البدء

```powershell
# للتحقق من البورت 3000
netstat -ano | findstr :3000

# إذا كان فارغاً، يمكنك البدء
# إذا كان مستخدماً، أوقف العملية أولاً
```

### 2. استخدم بورت مختلف للاختبار

```bash
# في package.json، أضف:
"start:3001": "next start -p 3001"

# ثم استخدم:
npm run start:3001
```

### 3. إنشاء script تلقائي

```powershell
# إنشاء ملف kill-port.ps1
# Kill process on port 3000
$port = 3000
$process = Get-NetTCPConnection -LocalPort $port -ErrorAction SilentlyContinue
if ($process) {
    Stop-Process -Id $process.OwningProcess -Force
    Write-Host "Killed process on port $port"
} else {
    Write-Host "No process found on port $port"
}
```

---

## ✅ قائمة التحقق

- [x] إيقاف العملية المستخدمة للبورت 3000
- [ ] بناء المشروع (`npm run build`)
- [ ] بدء Production Server (`npm start`)
- [ ] اختبار التنقل في Production
- [ ] قياس الأداء

---

*آخر تحديث: ديسمبر 2024*



