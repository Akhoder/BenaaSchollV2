# 🤖 دليل إعداد المساعد الذكي - AI Assistant Setup Guide

## ✅ ما تم إنجازه

تم إنشاء مساعد ذكي للطلاب يتضمن:

1. ✅ **API Route** (`/app/api/ai/chat/route.ts`)
   - يتعامل مع طلبات المحادثة
   - يتكامل مع OpenAI API أو Google Gemini
   - يحفظ المحادثات في قاعدة البيانات

2. ✅ **مكون واجهة المحادثة** (`/components/AIChatInterface.tsx`)
   - واجهة محادثة حديثة وسهلة الاستخدام
   - دعم متعدد اللغات (عربي، إنجليزي، فرنسي)
   - حفظ تلقائي للمحادثات

3. ✅ **صفحة المساعد الذكي** (`/app/dashboard/ai-assistant/page.tsx`)
   - صفحة مخصصة للطلاب فقط
   - واجهة احترافية مع PageHeader

4. ✅ **رابط في القائمة الجانبية**
   - تم إضافة رابط "المساعد الذكي" في قائمة الطلاب
   - أيقونة Bot مميزة

5. ✅ **قاعدة البيانات**
   - جدول `ai_conversations` لحفظ المحادثات
   - جدول `ai_chat_messages` لحفظ الرسائل
   - RLS policies للأمان

6. ✅ **الترجمات**
   - إضافة ترجمات للمساعد الذكي في جميع اللغات

---

## 🔧 الإعداد المطلوب

### 1. إضافة API Key

أضف أحد المفاتيح التالية في ملف `.env.local`:

#### خيار 1: استخدام OpenAI
```env
OPENAI_API_KEY=sk-your-openai-api-key-here
```

#### خيار 2: استخدام Google Gemini
```env
GEMINI_API_KEY=your-gemini-api-key-here
```

**ملاحظة:** إذا كان لديك كلا المفتاحين، سيتم استخدام OpenAI أولاً.

### 2. تشغيل Migration

قم بتشغيل migration لإنشاء الجداول:

```bash
# إذا كنت تستخدم Supabase CLI محلياً
supabase migration up

# أو قم بتشغيل الملف يدوياً في Supabase Dashboard
# الملف: supabase/migrations/20251119002000_create_ai_chat_tables.sql
```

### 3. الحصول على API Key

#### للحصول على OpenAI API Key:
1. اذهب إلى [OpenAI Platform](https://platform.openai.com/)
2. سجل الدخول أو أنشئ حساب
3. اذهب إلى API Keys
4. أنشئ مفتاح جديد
5. انسخ المفتاح وأضفه في `.env.local`

#### للحصول على Google Gemini API Key:
1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. سجل الدخول بحساب Google
3. أنشئ API Key جديد
4. انسخ المفتاح وأضفه في `.env.local`

---

## 🚀 الاستخدام

### للطلاب:

1. سجل الدخول كطالب
2. اذهب إلى القائمة الجانبية
3. اضغط على "المساعد الذكي" (أو "AI Assistant")
4. ابدأ المحادثة!

### الميزات:

- ✅ **إجابة على الأسئلة الأكاديمية**: اسأل أي سؤال عن المواد الدراسية
- ✅ **شرح المفاهيم**: اطلب شرح أي مفهوم صعب
- ✅ **مساعدة في الواجبات**: احصل على تلميحات (بدون حل مباشر)
- ✅ **تخطيط الدراسة**: اطلب مساعدة في تنظيم وقتك
- ✅ **تحليل الأداء**: اسأل عن أدائك الأكاديمي

---

## 📊 البيانات المستخدمة

المساعد الذكي يستخدم بيانات الطالب التالية:

- الفصول المسجل فيها
- المواد الدراسية
- المتوسط الحالي (إن وجد)
- الواجبات القادمة

هذه البيانات تُرسل تلقائياً للمساعد لتقديم إجابات أكثر دقة وملاءمة.

---

## 🔒 الأمان

- ✅ **RLS Policies**: فقط الطالب يمكنه رؤية محادثاته
- ✅ **Authentication**: يجب تسجيل الدخول للوصول
- ✅ **Role Check**: فقط الطلاب يمكنهم استخدام المساعد
- ✅ **Data Privacy**: المحادثات محفوظة بشكل آمن

---

## 🐛 استكشاف الأخطاء

### المشكلة: "AI API key not configured"

**الحل:**
- تأكد من إضافة `OPENAI_API_KEY` أو `GEMINI_API_KEY` في `.env.local`
- أعد تشغيل خادم التطوير

### المشكلة: "This feature is only available for students"

**الحل:**
- تأكد من أن المستخدم لديه دور "student"
- فقط الطلاب يمكنهم استخدام المساعد

### المشكلة: "Failed to send message"

**الحل:**
- تحقق من اتصال الإنترنت
- تحقق من صحة API Key
- تحقق من وجود رصيد في حساب OpenAI/Gemini

### المشكلة: الجداول غير موجودة

**الحل:**
- قم بتشغيل migration:
  ```bash
  supabase migration up
  ```
- أو قم بتشغيل SQL يدوياً في Supabase Dashboard

---

## 💰 التكلفة

### OpenAI:
- **GPT-3.5-turbo**: ~$0.002 لكل 1K tokens
- **تكلفة متوسطة**: ~$5-20/شهر (حسب الاستخدام)

### Google Gemini:
- **مجاني**: حتى 60 طلب/دقيقة
- **Pro**: $0.00025 لكل 1K tokens (أرخص من OpenAI)

**نصيحة:** ابدأ بـ Gemini لأنه مجاني للاستخدام الأساسي.

---

## 🔄 التحديثات المستقبلية

ميزات مقترحة:

- [ ] حفظ المحادثات محلياً (localStorage)
- [ ] تصدير المحادثات كـ PDF
- [ ] اقتراحات أسئلة ذكية
- [ ] دعم الصور في المحادثات
- [ ] تحليل المشاعر
- [ ] إحصائيات الاستخدام

---

## 📝 ملاحظات مهمة

1. **الخصوصية**: المحادثات محفوظة في قاعدة البيانات الخاصة بك
2. **الدقة**: المساعد قد يقدم معلومات غير دقيقة أحياناً - استخدمه كأداة مساعدة فقط
3. **التكلفة**: راقب استخدامك للـ API لتجنب التكاليف غير المتوقعة
4. **الحدود**: بعض APIs لديها حدود على عدد الطلبات - تحقق من وثائق API

---

## 🎉 جاهز للاستخدام!

المساعد الذكي جاهز الآن! فقط أضف API Key وابدأ الاستخدام.

**تاريخ الإنشاء**: 2025-01-19
**الإصدار**: 1.0

