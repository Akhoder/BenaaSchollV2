# مواقيت الصلاة - Prayer Times Feature ⏰🕌

## نظرة عامة
تم إضافة ميزة عرض مواقيت الصلاة لمدينة طرابلس - لبنان بشكل احترافي وتفاعلي في:
- ✅ **لوحة التحكم (Dashboard)** - للمستخدمين المسجلين
- ✅ **صفحة الهبوط (Landing Page)** - للزوار

## الملفات المضافة

### 1. `lib/prayerTimes.ts`
- يحتوي على بيانات مواقيت الصلاة لعام 2020 (366 يوم)
- تم تحويل البيانات تلقائياً من ملف `prayer/TimeData.dart`
- يوفر دوال مساعدة:
  - `getTodayPrayerTimes()`: الحصول على مواقيت الصلاة لليوم الحالي
  - `getPrayerTimesForDate(date)`: الحصول على مواقيت صلاة تاريخ معين
  - `getNextPrayer(prayerTime)`: معرفة الصلاة القادمة والوقت المتبقي
  - `getPrayerStatus(time)`: تحديد حالة الصلاة (قادمة، جارية، انتهت)

### 2. `components/PrayerTimesCard.tsx` ⭐
مكون React احترافي **محسّن** لعرض مواقيت الصلاة في Dashboard يتضمن:

#### 🎨 التحسينات الجديدة:
- **تصميم عصري** مع gradients متقدمة
- **أنيميشن سلس** للظهور والتفاعل
- **تأثيرات hover** احترافية
- **بطاقة الصلاة القادمة** مميزة بشكل خاص
- **نقاط مضيئة متحركة** للصلاة القادمة
- **خلفيات منقطة** ومتدرجة
- **أيقونات دوارة** عند hover
- **Shadow effects** متقدمة

#### المميزات الرئيسية:
- ✅ عرض جميع الصلوات الخمس + الشروق
- ✅ تمييز الصلاة القادمة بشكل بارز
- ✅ عداد تنازلي للوقت المتبقي للصلاة القادمة
- ✅ أيقونات مميزة لكل صلاة (قمر للفجر، شمس للظهر، إلخ)
- ✅ تحديث تلقائي كل دقيقة
- ✅ عرض التاريخ الهجري
- ✅ دعم كامل للغتين العربية والإنجليزية
- ✅ تصميم responsive يتكيف مع جميع الشاشات
- ✅ Dark mode support
- ✅ حالات الصلاة (قادمة، جارية، انتهت) بألوان مختلفة

#### المكونات الفرعية:
- **`PrayerTimesCard`**: المكون الرئيسي الكامل للـ Dashboard
- **`PrayerTimesCompact`**: نسخة مصغرة محسّنة للـ sidebar
- **`PrayerTimesLanding`**: نسخة خاصة لصفحة الهبوط (جديد!)

### 3. `components/PrayerTimesLanding.tsx` 🌟 **جديد!**
مكون خاص مصمم لصفحة الهبوط مع:

#### ✨ المميزات الفريدة:
- **تخطيط مميز**: Grid Layout مع بطاقة كبيرة للصلاة القادمة + شبكة للباقي
- **خلفيات متحركة**: Blob animations متقدمة
- **تأثيرات بصرية**: Glow effects & Backdrop blur
- **Sticky positioning**: البطاقة الكبيرة ثابتة عند التمرير
- **نقاط مضيئة**: Ping animations للصلاة القادمة
- **تدرجات لونية**: من Emerald إلى Teal إلى Cyan
- **Responsive design**: يتكيف من 1 عمود إلى 3 أعمدة
- **Fade-in animations**: ظهور تدريجي للعناصر

### 4. التحديثات على الملفات الموجودة

#### `lib/translations.ts`
تم إضافة الترجمات التالية بثلاث لغات (عربي، إنجليزي، فرنسي):
```typescript
prayerTimes: 'مواقيت الصلاة' / 'Prayer Times' / 'Heures de prière'
nextPrayer: 'الصلاة القادمة' / 'Next Prayer' / 'Prochaine prière'
fajr: 'الفجر' / 'Fajr'
sunrise: 'الشروق' / 'Sunrise' / 'Lever du soleil'
dhuhr: 'الظهر' / 'Dhuhr'
asr: 'العصر' / 'Asr'
maghrib: 'المغرب' / 'Maghrib'
isha: 'العشاء' / 'Isha'
tripoliLebanon: 'طرابلس، لبنان' / 'Tripoli, Lebanon' / 'Tripoli, Liban'
// ... والمزيد
```

#### `app/dashboard/page.tsx`
- تم استيراد `PrayerTimesCard`
- تم إضافة المكون في القسم الخاص بالطلاب (Student Dashboard)
- الموقع: بعد قسم "Today's Schedule and Quick Actions" وقبل "Upcoming Assignments"

#### `app/page.tsx` 🆕
- تم استيراد `PrayerTimesLanding`
- تم إضافة المكون في صفحة الهبوط الرئيسية
- الموقع: بعد قسم "About" وقبل قسم "Subjects"
- محاط بـ Islamic Dividers للتناسق مع التصميم الإسلامي

## كيفية الاستخدام

### العرض في Dashboard
المكون يظهر تلقائياً في لوحة التحكم للطلاب. لإضافته في صفحات أخرى:

```tsx
import { PrayerTimesCard } from '@/components/PrayerTimesCard';

function MyPage() {
  return (
    <div>
      <PrayerTimesCard />
    </div>
  );
}
```

### النسخة المصغرة
للاستخدام في sidebar أو مساحات ضيقة:

```tsx
import { PrayerTimesCompact } from '@/components/PrayerTimesCard';

function MySidebar() {
  return (
    <div>
      <PrayerTimesCompact />
    </div>
  );
}
```

### نسخة صفحة الهبوط
للاستخدام في صفحات الهبوط أو الصفحات الترويجية:

```tsx
import { PrayerTimesLanding } from '@/components/PrayerTimesLanding';

function LandingPage() {
  return (
    <main>
      {/* محتوى آخر */}
      <PrayerTimesLanding />
      {/* محتوى آخر */}
    </main>
  );
}
```

### استخدام دوال المساعدة
```typescript
import { getTodayPrayerTimes, getNextPrayer } from '@/lib/prayerTimes';

const today = getTodayPrayerTimes();
if (today) {
  const next = getNextPrayer(today);
  console.log(`الصلاة القادمة: ${next?.nameAr} في ${next?.time}`);
}
```

## التخصيص

### تغيير الألوان
يمكنك تخصيص الألوان في `components/PrayerTimesCard.tsx`:

```tsx
// الصلاة القادمة
className="bg-primary text-primary-foreground"

// الصلاة الجارية
className="ring-2 ring-primary"

// الصلاة المنتهية
className="opacity-60"
```

### إضافة مدن أخرى
لإضافة بيانات مواقيت صلاة لمدن أخرى:

1. احصل على ملف TimeData.dart للمدينة الجديدة
2. قم بنسخ البيانات إلى ملف جديد مثل `lib/prayerTimesJeddah.ts`
3. عدّل دالة `getTodayPrayerTimes()` لتقبل معامل المدينة

## البيانات

### المصدر
البيانات الأصلية من ملف `prayer/TimeData.dart` لمدينة طرابلس - لبنان

### التغطية
- السنة: 2020 (366 يوم - سنة كبيسة)
- عند استخدام البيانات في سنوات أخرى، يتم تلقائياً استخدام نفس اليوم والشهر من 2020

### الدقة
المواقيت محسوبة وفق معايير موثوقة ومناسبة لمدينة طرابلس - لبنان

## التحسينات المستقبلية المقترحة

1. ✨ **API Integration**: ربط بـ API خارجي للحصول على مواقيت محدثة
2. 🔔 **Notifications**: إشعارات قبل موعد الصلاة
3. 🌍 **Multi-City Support**: دعم مدن متعددة
4. 📱 **Widget**: إضافة widget صغير في الـ header
5. 🎵 **Adhan Audio**: تشغيل الأذان عند دخول الوقت
6. 📊 **Statistics**: إحصائيات الصلوات المؤداة
7. 🗓️ **Calendar Integration**: دمج مع تقويم Google/Outlook

## الأداء

- ⚡ **التحديث**: كل دقيقة (60 ثانية)
- 💾 **حجم البيانات**: ~50KB (366 سجل)
- 🚀 **التحميل**: lazy loading مع Suspense
- 📦 **Bundle Size**: ~5KB (gzipped)

## الدعم

- ✅ React 18+
- ✅ Next.js 13+ (App Router)
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ shadcn/ui components

## المطورون

تم التطوير بواسطة فريق مدرسة البناء العلمي

## الترخيص

نفس ترخيص المشروع الأساسي

---

## 🎨 التحديث الأخير: التصميم المحسّن

### التاريخ
تم تحسين التصميم بشكل كامل مع إضافة نسخة خاصة لصفحة الهبوط

### ✨ التحسينات الجديدة

#### 1. **PrayerTimesCard** (Dashboard)
- تدرجات لونية متقدمة (Emerald → Teal → Cyan)
- أنيميشن slideIn للعناصر
- نقاط مضيئة متحركة (ping animation)
- تأثيرات hover احترافية
- بطاقة الصلاة القادمة مميزة بشكل خاص
- خلفيات منقطة ومتدرجة
- Shadow effects متقدمة

#### 2. **PrayerTimesCompact** (Sidebar)
- تصميم مصغر محسّن
- خلفية متحركة
- Backdrop blur
- عرض مباشر للصلاة القادمة فقط

#### 3. **PrayerTimesLanding** (Landing Page) 🆕
- تخطيط Grid مميز (1x2 columns)
- بطاقة كبيرة sticky للصلاة القادمة
- شبكة responsive للصلوات الأخرى
- خلفيات blob متحركة
- Fade-in animations متدرجة
- Islamic patterns في الخلفية
- تدرجات لونية من Emerald إلى Cyan

### 📍 الأماكن المضافة

1. **Dashboard** (`/dashboard`)
   - في قسم الطلاب
   - بعد Schedule & Quick Actions
   - قبل Upcoming Assignments

2. **Landing Page** (`/`)
   - بعد قسم About
   - قبل قسم Subjects
   - محاط بـ Islamic Dividers

### 📦 الملفات المحدثة

```
✅ components/PrayerTimesCard.tsx      (محسّن بالكامل)
✅ components/PrayerTimesLanding.tsx   (جديد!)
✅ app/dashboard/page.tsx              (إضافة المكون)
✅ app/page.tsx                        (إضافة المكون)
✅ docs/PRAYER_TIMES.md                (تحديث التوثيق)
```

### 🎯 النتيجة النهائية

المستخدمون سيرون الآن:
- **في صفحة الهبوط**: عرض مواقيت الصلاة بشكل بارز وجذاب لجميع الزوار
- **في Dashboard**: بطاقة مدمجة ومميزة للطلاب المسجلين
- **تصميم موحد**: نفس الألوان والأسلوب عبر جميع المكونات
- **تجربة سلسة**: Animations وتأثيرات بصرية احترافية
- **📅 التاريخ الميلادي**: يُعرض التاريخ الحالي بالكامل
- **🌙 التاريخ الهجري**: يُعرض من البيانات الأصلية الموثوقة

