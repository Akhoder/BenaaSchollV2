# 🔧 استكشاف أخطاء المساعد الذكي - Troubleshooting Guide

## ❌ خطأ: "models/gemini-xxx is not found"

### المشكلة:
اسم النموذج غير متوفر في مشروعك.

### الحل:
تم تحديث الكود ليحاول **تلقائياً** عدة نماذج وإصدارات API:

**النماذج التي سيحاولها بالترتيب:**
1. `gemini-2.5-flash` (v1) - الأحدث والأسرع ⭐
2. `gemini-2.5-pro` (v1) - الأحدث والأقوى ⭐
3. `gemini-2.0-flash` (v1) - سريع ومستقر
4. `gemini-2.0-flash-001` (v1) - نسخة مستقرة
5. `gemini-2.5-flash-lite` (v1) - خفيف الوزن
6. `gemini-2.0-flash-lite-001` (v1) - خفيف ومستقر
7. نماذج قديمة (fallback)

**الميزات:**
- ✅ يحاول تلقائياً جميع النماذج المتاحة
- ✅ يعمل مع أي نموذج متوفر في مشروعك
- ✅ لا حاجة لأي إجراء - الكود محدث تلقائياً!

### التحقق من النماذج المتاحة:

يمكنك التحقق من النماذج المتاحة في مشروعك عبر:

```bash
# في المتصفح أو Postman
GET http://localhost:3005/api/ai/list-models
```

أو افتح في المتصفح:
```
http://localhost:3005/api/ai/list-models
```

سيظهر لك قائمة بجميع النماذج المتاحة في مشروعك.

---

## ❌ خطأ: "You exceeded your current quota"

### المشكلة:
تم تجاوز الحصة المخصصة في OpenAI API.

### الحلول:

#### الحل 1: استخدام Google Gemini (مُوصى به - مجاني)
1. احصل على Gemini API Key من [Google AI Studio](https://makersuite.google.com/app/apikey)
2. أضف في `.env.local`:
   ```env
   GEMINI_API_KEY=your-gemini-key-here
   ```
3. أزل أو علّق `OPENAI_API_KEY`:
   ```env
   # OPENAI_API_KEY=sk-...  # معلق مؤقتاً
   GEMINI_API_KEY=your-gemini-key-here
   ```
4. أعد تشغيل الخادم

#### الحل 2: تحديث خطة OpenAI
1. اذهب إلى [OpenAI Platform](https://platform.openai.com/account/billing)
2. أضف بطاقة ائتمانية أو رصيد
3. انتظر حتى يتم تفعيل الحساب

#### الحل 3: استخدام Fallback تلقائي
الكود الآن يدعم Fallback تلقائي - إذا فشل OpenAI، سيحاول استخدام Gemini تلقائياً (إذا كان متوفراً).

---

## 🔑 إعداد Google Gemini (مجاني)

### الخطوات:

1. **احصل على API Key:**
   - اذهب إلى: https://makersuite.google.com/app/apikey
   - سجل الدخول بحساب Google
   - اضغط "Create API Key"
   - انسخ المفتاح

2. **أضف في `.env.local`:**
   ```env
   GEMINI_API_KEY=your-key-here
   ```

3. **أعد تشغيل الخادم:**
   ```bash
   npm run dev
   ```

### المميزات:
- ✅ **مجاني**: حتى 60 طلب/دقيقة
- ✅ **لا يحتاج بطاقة ائتمانية**
- ✅ **جودة ممتازة**: مشابه لـ GPT-3.5
- ✅ **Fallback تلقائي**: إذا فشل OpenAI

---

## 🔄 كيف يعمل Fallback

الكود الآن يدعم:

1. **الأولوية 1**: Google Gemini (إذا كان متوفراً)
2. **الأولوية 2**: OpenAI (إذا كان متوفراً)
3. **Fallback**: إذا فشل OpenAI، يحاول Gemini تلقائياً

### مثال:
```env
# .env.local
OPENAI_API_KEY=sk-...  # قد يكون quota منتهي
GEMINI_API_KEY=your-key-here  # سيستخدم هذا تلقائياً
```

---

## ✅ التحقق من الإعداد

### 1. تحقق من الملفات:
```bash
# تأكد من وجود الملف
cat .env.local

# يجب أن يحتوي على واحد على الأقل:
# OPENAI_API_KEY=sk-...
# أو
# GEMINI_API_KEY=...
```

### 2. تحقق من الخادم:
```bash
# أعد تشغيل الخادم بعد إضافة المفتاح
npm run dev
```

### 3. اختبر المساعد:
- سجل الدخول كطالب
- اذهب إلى "المساعد الذكي"
- اكتب رسالة واختبر

---

## 🐛 أخطاء شائعة أخرى

### خطأ: "API key not configured"
**الحل:**
- تأكد من وجود `.env.local` في جذر المشروع
- تأكد من صحة اسم المتغير (`OPENAI_API_KEY` أو `GEMINI_API_KEY`)
- أعد تشغيل الخادم بعد إضافة المفتاح

### خطأ: "Unauthorized"
**الحل:**
- تحقق من صحة API Key
- تأكد من أن المفتاح لم ينتهِ صلاحيته
- في حالة Gemini، تأكد من تفعيل API في Google Cloud Console

### خطأ: "Rate limit exceeded"
**الحل:**
- انتظر قليلاً (Gemini: 60 طلب/دقيقة)
- قلل من عدد الطلبات
- استخدم OpenAI إذا كان لديك خطة أعلى

---

## 💡 نصائح

1. **ابدأ بـ Gemini**: مجاني ولا يحتاج بطاقة
2. **راقب الاستخدام**: استخدم console.log لمراقبة أي API يتم استخدامه
3. **احتفظ بنسخة احتياطية**: احتفظ بكلا المفتاحين للـ fallback
4. **اختبر أولاً**: اختبر مع رسالة بسيطة قبل الاستخدام المكثف

---

## 📊 مقارنة سريعة

| الميزة | OpenAI | Google Gemini |
|--------|--------|---------------|
| **التكلفة** | مدفوع | مجاني (60 طلب/دقيقة) |
| **الجودة** | ممتازة | ممتازة |
| **السرعة** | سريع | سريع |
| **اللغة العربية** | جيد | جيد جداً |
| **يحتاج بطاقة** | نعم | لا |

**التوصية:** ابدأ بـ Gemini للمشاريع الصغيرة/التجريبية.

---

## 🔗 روابط مفيدة

- [Google AI Studio](https://makersuite.google.com/app/apikey)
- [OpenAI Platform](https://platform.openai.com/)
- [Gemini API Docs](https://ai.google.dev/docs)
- [OpenAI API Docs](https://platform.openai.com/docs)

---

**آخر تحديث**: 2025-01-19

